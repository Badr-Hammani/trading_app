import { StrategyLabView } from './StrategyLabView';
export const metadata = { title: 'Strategy Lab · XAUUSD Command Center' };
export default function StrategyLabPage() {
  return <StrategyLabView />;
}
